= template_frame_component(src: @referrer_path) do |frame|
  - frame.with_modal do
    = render Views::Users::FormComponent.new(@user, scope: :admin, organization: @collectivity, redirection_path: @referrer_path)
