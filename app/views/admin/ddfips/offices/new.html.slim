= template_frame_component(src: @referrer_path) do |frame|
  - frame.with_modal do
    = render Views::Offices::FormComponent.new(@office, scope: :admin, ddfip: @ddfip, redirection_path: @referrer_path)
