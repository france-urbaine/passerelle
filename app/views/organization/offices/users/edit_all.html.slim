= template_frame_component(src: @referrer_path) do |frame|
  - frame.with_modal do
    = render Views::Offices::EditUsersComponent.new(@office, scope: :organization, redirection_path: @referrer_path)
