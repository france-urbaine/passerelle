<% if transmission&.reports&.any? %>
  <%= button_component(transmissions_path, modal: true) do %>
    Transmission en attente
    <%= counter_component(transmission.reports.count) %>
  <% end %>
<% end %>
