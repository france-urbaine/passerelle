<%= turbo_stream.update dom_id(@transmission, :button), partial: "transmissions/transmission_button", locals: { transmission: @transmission } %>

<%# Watch out when there're many removable_reports %>
<% @result.removed_reports.find_each do |report| %>
  <turbo-stream action="update" target="<%= dom_id(report, :status) %>">
    <template>
      <%= report_badge(report) %>
    </template>
  </turbo-stream>
<% end %>

<%= turbo_stream.update "modal", partial: "transmissions/destroy" %>
