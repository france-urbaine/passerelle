<%= turbo_stream.update dom_id(@transmission), partial: "transmissions/transmission_button", locals: { transmission: @transmission } %>

<% @transmissible_reports.each do |report| %>
  <turbo-stream action="update" target="status_report_<%= report.id %>">
    <template>
      <%= report_badge(report) %>
    </template>
  </turbo-stream>
<% end %>

<%= turbo_stream.update "modal", partial: "transmissions/create" %>
