= template_frame_component(src: @referrer_path) do |frame|
  - frame.with_modal do |modal|
    - modal.with_header do
      | Suppression de l'éditeur : #{@publisher.name}

    - modal.with_form(model: @publisher, method: :delete) do
      p
        | Êtes-vous sûrs de vouloir supprimer cet éditeur ?
        br
        | Le compte de l'éditeur sera archivé pendant 30 jours avant d'être définitivement supprimé.

    - modal.with_submit_action "Continuer", autofocus: true
    - modal.with_close_action "Annuler"
